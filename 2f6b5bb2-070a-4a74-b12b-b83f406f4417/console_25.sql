create schema way;

set search_path to 'way';

create table if not exists way.what
(
    what_id        int4      not null
        constraint what_pk primary key generated by default as identity,
    db_create_time timestamp not null default clock_timestamp()
);

create table way.